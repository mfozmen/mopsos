/**
 * Placeholder entry point. Phase 0 is infrastructure only — no product code yet.
 * The core (verdict schemas, Brier scoring, asset class registry) arrives in Phase 1.
 */
export const PROJECT_NAME = 'mopsos';
